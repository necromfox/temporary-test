import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-electron-solid-vite-universal-template',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
